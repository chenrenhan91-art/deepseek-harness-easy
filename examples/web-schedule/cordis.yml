# The shipped Web composition already loads time-context and schedule.
# Restating the rows keeps `dsh web --patch examples/web-schedule/cordis.yml`
# valid against that composition.

- id: time-context
  name: '@deepseek-ai/dsh-time-context'

- id: schedule
  name: '@deepseek-ai/dsh-schedule'
